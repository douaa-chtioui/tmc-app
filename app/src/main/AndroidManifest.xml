<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tmc.ensi.org.tmcapp">

    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />
    <uses-permission android:name="com.androidtutorialpoint.mymapsappsdirection.permission.MAPS_RECEIVE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignupActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".ProfileActivity" />

        <receiver android:name=".NotificationBroadcastReceiver" />

        <activity android:name=".CheckupActivity" />
        <activity android:name=".CheckupDetails" />
        <activity android:name=".CheckupForm" />
        <activity android:name=".DoctorCodeActivity" />
        <activity android:name=".MedicalProblemsActivity" />
        <activity android:name=".ChronicDiseaseActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity2"
            android:label="@string/title_activity_maps2" />
        <activity android:name=".DoctorHomeActivity" />
        <activity android:name=".CoronaryPathologyActivity" />
        <activity android:name=".ValvePathologyActivity" />
        <activity android:name=".HeartRhythmPathologyActivity" />
        <activity android:name=".RiskCalculatorActivity" />
        <activity android:name=".HeartFailureActivity" />
        <activity
            android:name=".UserHomeActivity"
            android:label="Home"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Help"
            android:label="@string/title_activity_help"
            android:theme="@style/AppTheme" />
        <activity android:name=".Height" />
        <activity
            android:name=".AboutAs"
            android:label="@string/title_activity_about_as"
            android:theme="@style/AppTheme" />
        <activity android:name=".Weight" />
        <activity android:name=".Qgender" />
        <activity android:name=".QMenopause" />
        <activity android:name=".QSmoking" />
        <activity android:name=".QDiabetes" />
        <activity android:name=".QRenalDiseeas" />
        <activity android:name=".QHyperTension" />
        <activity android:name=".QDyslipidemia" />
        <activity android:name=".QFamilyAntecedent" />
        <activity android:name=".FinalProfilActivity" />
        <activity android:name=".QAge" />
        <activity
            android:name=".ContactAs"
            android:label="@string/title_activity_contact_as"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>